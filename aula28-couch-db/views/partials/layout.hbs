<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8"/>
        <title>{{title}}</title>
        <link rel="stylesheet" href="/assets/css/bootstrap.min.css" />
	</head>
    <body>
		<div class="container">
			<div class="row">
				<div class="panel col-xs-3">
					{{#if @user}}
						{{> favourites}}
					{{else}}
						<a class="center-block btn btn-primary" href='/login'>Login</a>
					{{/if}}
				</div>
				<div class="col-xs-9">
					{{> @partial-block}}
				</div>
			</div>
		</div>
    </body>
</html>
